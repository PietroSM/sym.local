<nav class="navbar navbar-fixed-top navbar-default">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#menu">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand page-scroll" href="#page-top">
				<span>[PHOTO]</span>
			</a>
		</div>
		<div class="collapse navbar-collapse navbar-right" id="menu">
			<ul class="nav navbar-nav">

				{% if app.request.pathinfo=='/index' or app.request.pathinfo=='/' %}
					<li class="active lien">
				{% else %}
					<li class="lien">
				{% endif %}
					<a href={{ path('sym_index') }}>
					<i class="fa fa-home sr-icons"></i>Home</a>
				</li>

				{% if app.request.pathinfo=='/about' %}
					<li class="active lien">
				{% else %}
					<li class="lien">
				{% endif %}
					<a href={{ path('sym_about') }}>
					<i class="fa sr-icons"></i>About</a>
				</li>

				{% if app.request.pathinfo=='/blog' %}
					<li class="active lien">
				{% else %}
					<li class="lien">
				{% endif %}
					<a href={{ path('sym_blog') }}>
					<i class="fa sr-icons"></i>Blog</a>
				</li>

				{% if app.request.pathinfo=='/contact' %}
					<li class="active lien">
				{% else %}
					<li class="lien">
				{% endif %}
					<a href={{ path('sym_contact') }}>
					<i class="fa sr-icons"></i>Contact</a>
				</li>


				{% if app.request.pathinfo=='/imagen' %}
					<li class="active lien">
				{% else %}
					<li class="lien">
				{% endif %}
					<a href={{ path('sym_imagen') }}>
					<i class="fa sr-icons"></i>Imagen</a>
				</li>


			</ul>
		</div>
	</div>
</nav>
